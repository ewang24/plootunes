export class Icons{
    static BACK_ARROW = Object.freeze("backArrow.svg");
    static PLAY = Object.freeze("play");
    static PLUS = Object.freeze("plus");
    static STOP = Object.freeze("stop");
    static X = Object.freeze("x");
    static HAMBURGER = Object.freeze("hamburger");
    static SHUFFLE = Object.freeze("shuffle");
    static REPEAT_CIRCLE = Object.freeze("repeatCircle");
    static REWIND = Object.freeze("rewind")
    static FAST_FORWARD = Object.freeze("fastForward")
}